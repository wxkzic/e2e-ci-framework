# e2e-ci-framework

Unified E2E framework: **Playwright (UI)** + **Postman/Newman (API)** with HTML reports and GitHub Actions CI.

## Local
```bash
npm install
npm run test:ui
npm run test:api
npm run report:ui
npm run report:api
